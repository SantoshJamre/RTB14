<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>New Book Added: {{bookTitle}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .container {
            border: 1px solid #e0e0e0;
            border-radius: 5px;
            padding: 20px;
        }
        .header {
            background-color: #4a90e2;
            color: white;
            padding: 15px 20px;
            border-radius: 4px 4px 0 0;
            margin: -20px -20px 20px -20px;
        }
        .book-details {
            background-color: #f9f9f9;
            padding: 15px;
            border-radius: 4px;
            margin: 15px 0;
        }
        .book-title {
            font-size: 20px;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 10px;
        }
        .book-info {
            margin: 5px 0;
            color: #555;
        }
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #777;
            border-top: 1px solid #eee;
            padding-top: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>New Book Added to Library</h2>
        </div>
        
        <p>Hello {{firstName}},</p>
        <p>We're excited to let you know that a new book has been added to our library!</p>
        
        <div class="book-details">
            <div class="book-title">{{bookTitle}}</div>
            <div class="book-info"><strong>Author:</strong> {{bookAuthor}}</div>
            {{#if bookCategory}}
            <div class="book-info"><strong>Category:</strong> {{bookCategory}}</div>
            {{/if}}
            {{#if bookPublishedDate}}
            <div class="book-info"><strong>Published Date:</strong> {{bookPublishedDate}}</div>
            {{/if}}
            <div class="book-info"><strong>Added by:</strong> {{addedBy}}</div>
        </div>
        
        <p>Visit the library to check out this exciting new addition to our collection!</p>
        
        <div class="footer">
            <p>Best regards,<br>RTB-14 Library Team</p>
        </div>
    </div>
</body>
</html>
